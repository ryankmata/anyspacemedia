"use client";
import Showcase from "../carousel";

const SpaceSelection = () => {
  return (
    <section id="choose_your_space" className="scroll-mt-[25px] min-h-screen">
      <Showcase />
    </section>
  );
};

export default SpaceSelection;
